export const environment = {
    production: false,
    // apiUrl: window.location.hostname === 'localhost' ? 'http://localhost:3000' : 'https://jsonplaceholder.typicode.com'
    apiUrl: window.location.hostname === 'localhost' ? 'https://jsonplaceholder.typicode.com' : 'https://jsonplaceholder.typicode.com'
};